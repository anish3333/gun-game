# k6 load test config
# k6 does NOT load .env files automatically — use:
#   ./run-loadtest.sh
# or:
#   export $(grep -v '^#' loadtest.env | xargs) && k6 run loadtest.js

BASE_URL=http://localhost:3000
VUS=50
DURATION=45s
SCORE_LIMIT=3
MATCH_DURATION_MS=35000
STAGGER_MAX_S=5